# 数据库连接字符串
# 开发环境：SQLite（默认）
# 生产环境：修改为生产数据库路径，例如 file:/var/www/flow-calendar/data/prod.db
DATABASE_URL="file:./dev.db"

# API 跨域白名单（Nginx 反代部署时配置）
# 例如：ALLOWED_ORIGINS="flow-calendar.gouxinjie.com"
ALLOWED_ORIGINS=""
